import "protocols/net.proto";
package routesrv;

message Connect {
  repeated uint32 shards = 1 [packed=true];
}

message Disconnect {}

message Heartbeat {
  repeated uint32 shards = 1 [packed=true];
}
